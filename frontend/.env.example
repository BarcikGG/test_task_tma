# ── Runtime ─────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production

# API_URL is injected at build time via Docker build arg (see docker-compose.yml)
# Inside Docker: http://server:5094
# Local dev:     http://localhost:5094  (default in next.config.ts)
